# Normalize line endings: text files are LF in the repo, checked out per-platform.
* text=auto

# Explicitly binary
*.png binary
*.ico binary
*.ttf binary
*.db binary
*.onnx binary

# Windows-toolchain files stay CRLF on checkout
*.sln text eol=crlf
*.iss text eol=crlf
